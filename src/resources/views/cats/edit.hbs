<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <h3 class="text-center mb-4">Chỉnh sửa thông tin mèo</h3>
            <form method="post" action="/cat/{{Cat._id}}?_method=put">
                <div class="form-group">
                    <label for="name">Tên mèo</label>
                    <input type="text" class="form-control" value="{{Cat.name}}" id="name" placeholder="Nhập tên mèo" name="name" required>
                </div>
                <div class="form-group">
                    <label for="image">Ảnh</label>
                    <input type="text" class="form-control" value="{{Cat.image}}" id="image" placeholder="Nhập link ảnh" name="image" required>
                </div>
                <div class="form-group">
                    <label for="note">Giới thiệu</label>
                    <input type="text" class="form-control" value="{{Cat.note}}" id="note" placeholder="Nhập giới thiệu" name="note" required>
                </div>
                <div class="form-group">
                    <label for="description">Thông tin chi tiết</label>
                    <textarea class="form-control" id="description" placeholder="Nhập thông tin chi tiết" name="description" rows="4" required>{{Cat.description}}</textarea>
                </div>
                <button type="submit" class="btn btn-primary">Lưu lại</button>
            </form>
        </div>
    </div>
</div>
